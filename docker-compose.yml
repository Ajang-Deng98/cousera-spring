services:
  event-locator:
    build: .
    container_name: event-locator-app
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge